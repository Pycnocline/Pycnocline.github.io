---
import Fonts from "./Fonts.astro"
---
<head>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
</head>
<body>
    <header>
        <!-- Logo -->
        <input type="checkbox" name="check" id="check">
        <h1 class="logo">
            <a href="#">无限折腾(👉ﾟヮﾟ)👉</a>
        </h1>
        <h1 class="mini-logo">
            <a href="#">无限折腾</a>
        </h1>
        <nav class="nav">
            <h1 class="side-mini-logo">无限折腾</h1>
            <div class="side-divider"></div>
            <!-- 导航 -->
            <a href="#">回到首页</a>
            <a href="#">服务列表</a>
            <a href="#">个人博客</a>
            <a href="#">友情链接</a>
        </nav>
        <div class="function-group">
            <label for="check">
                <i class='bx bx-menu menu'></i>
                <i class='bx bx-x close-menu' ></i>
            </label>
        </div>
    </header>
</body>

<Fonts>

<style>
    * {
        margin: 0;
        padding: 0;
    }

    body {

    }

    header {
        left: 0;
        top: 0;
        position: fixed;
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: rgba(1, 4, 9, 0.5);
        box-shadow: 0px 2px 0px rgb(61, 68, 77);
        backdrop-filter: blur(10px);
        height: 80px;
        width: 100vw;
    }

    .logo a {
        margin-left: 16px;
        margin-right: 16px;
        color: rgb(240, 246, 252);
        font-family: '白路飞云手写体', sans-serif;
        text-decoration: none;
        transition: 300ms;
    }
    .logo a:hover {
        text-shadow: 2px 4px 6px rgba(0, 0, 0, 0.5);
    }

    .mini-logo {
        margin-left: 16px;
        display: none;
    }
    .mini-logo a {
        color: rgb(240, 246, 252);
        font-family: '白路飞云手写体', sans-serif;
        text-decoration: none;
        transition: 300ms;
    }
    .mini-logo a:hover {
        text-shadow: 2px 4px 6px rgba(0, 0, 0, 0.5);
    }

    .side-mini-logo {
        display: none;
        color: rgb(240, 246, 252);
        font-family: '白路飞云手写体', sans-serif;
        text-decoration: none;
        font-size: xx-large;
    }

    .nav a {
        color: rgb(240, 246, 252);
        text-decoration: none;
        font-weight: 400;
        font-size: 16px;
        transition: 300ms;
        padding: 8px 16px;
        border-radius: 5px;
    }
    .nav a:hover {
        background: rgba(255, 255, 255, 0.1);
    }

    #check {
        display: none;
    }

    .menu {
        display: none;
    }
    
    .close-menu {
        display: none;
    }

    .function-group {
        margin-right: 16px;
        justify-content: flex-end;
    }

    /* 当页面宽度小于850px时，将页面导航更改为侧边栏菜单 */
    @media (max-width: 850px) {
        .nav {
            position: absolute;
            left: -100%;
            top: 0;
            display: flex;
            flex-direction: column;
            background: rgb(13, 17, 23);
            height: 100vh;
            width: 45%;
            padding: 0px 20px;
            box-shadow: 0px 0px 12px rgba(0, 0, 0, 0.5);
            gap: 16px;
            transition: 500ms;
        }
        .side-mini-logo {
            display: block;
            margin-top: 24px;
            margin-left: 16px;
        }
        .side-divider {
            background: rgb(61, 68, 77);
            width: 100%;
            height: 2px;
        }
        .function-group {
            display:flex
        }
        .menu {
            display: block;
            font-size: 40px;
            color: rgb(240, 246, 252);
            margin-top: 5px;
            cursor: pointer;
        }
        #check:checked~.function-group .menu {
            display: none;
        }
        #check:checked~.nav {
            left: 0%;
        }
        .close-menu {
            font-size: 40px;
            color: rgb(240, 246, 252);
            margin-top: 5px;
            cursor: pointer;
        }
        .function-group .close-menu {
            display: none;
        }
        #check:checked~.function-group .close-menu {
            display: block;
        }
    }

    /* 当页面宽度小于512px时，将大页面logo替换为小logo */
    @media (max-width: 512px) {
        .logo {
            display: none;
        }
        .mini-logo {
            display: block;
        }
        .nav {
            width: 60%;
        }
    }
</style>